{
  "name": "top-50-llm-questions",
  "version": "1.0.0",
  "description": "Interactive LLM interview questions with semantic HTML/CSS patterns.",
  "private": true,
  "scripts": {
    "dev": "vite",
    "lint": "npm run lint:html && npm run lint:css && npm run lint:repo",
    "lint:html": "node scripts/lint-html.js",
    "lint:css": "stylelint \"css/**/*.css\"",
    "lint:repo": "node scripts/validate-references.js"
  },
  "devDependencies": {
    "fast-glob": "^3.3.2",
    "html-validate": "^9.4.0",
    "vite": "^7.1.8",
    "stylelint": "^16.9.0",
    "stylelint-config-standard": "^36.0.0"
  }
}
